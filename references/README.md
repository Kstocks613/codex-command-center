# References

This folder can hold source docs that inspired or constrain the workflow.

Included when available:

- `example_project_source.md` — the example project workflow.
- `council_skill_source.pdf` — the recursive AI council protocol source document.

Codex should use references as context, but should maintain working instructions in `AGENTS.md`, `docs/`, `prompts/`, and `.agents/skills/`.
